# Krpc 性能测试报告

## 一、测试指标说明

### 1. 延迟（Latency）

延迟指的是从客户端发起 RPC 请求到收到响应所经历的时间。我们使用百分位数（Percentile）来描述延迟分布，而不是简单的平均值，因为平均值会掩盖长尾问题。

| 指标 | 含义 | 举例 |
|------|------|------|
| **P50（中位数）** | 50% 的请求延迟低于该值 | P50=0.20ms 表示一半的请求在 0.20ms 内完成 |
| **P95** | 95% 的请求延迟低于该值 | P95=0.29ms 表示 95% 的请求在 0.29ms 内完成，剩下的 5% 更慢 |
| **P99** | 99% 的请求延迟低于该值 | P99=0.37ms 表示最慢的 1% 请求可能达到 0.37ms 以上 |
| **Min** | 最快的一次请求延迟 | — |
| **Max** | 最慢的一次请求延迟 | — |

**为什么要看 P95/P99 而不是只看平均值？**
假设 100 次请求中有 99 次耗时 1ms，1 次耗时 100ms，平均值是 ~2ms，看起来不错。但 P99 会暴露出 100ms 的长尾延迟，这对用户体验是致命的。在分布式系统中，P99 延迟决定了最坏情况下的用户体验。

### 2. QPS（Queries Per Second）

每秒处理的请求数，衡量系统的吞吐能力。

```
QPS = 总成功请求数 / 总耗时(秒)
```

例如 5000 个请求耗时 1.91 秒，则 QPS = 5000/1.91 = 2612。

QPS 和延迟通常是**反向关系**：随着并发度增加，QPS 上升，但每个请求的延迟也会变长（因为需要排队等待处理资源）。

### 3. 序列化对比指标

| 指标 | 含义 |
|------|------|
| **Size (B)** | 序列化后的字节数，越小网络传输开销越低 |
| **Serialize** | 将内存对象转为字节流所需时间 |
| **Deserialize** | 将字节流还原为内存对象所需时间 |
| **Ratio (JSON/PB)** | JSON 指标是 Protobuf 的多少倍，倍数越高说明 Protobuf 优势越大 |

---

## 二、测试方案

### 2.1 序列化对比测试（ser_bench）

**目的**：验证 Protobuf 相比 JSON 在体积和速度上的优势，用数据支撑 README 中的声明。

**测试方法**：
- 使用 `LoginRequest`（name="zhangsan", pwd="123456"）和 `RegisterRequest`（id=1001, name="zhangsan", pwd="123456"）作为测试样本
- 对每种消息类型，分别用 Protobuf 和 JSON (jsoncpp) 执行 500,000 次序列化/反序列化
- 记录总耗时、序列化后字节大小
- Protobuf 和 JSON 分别连续执行（先跑完所有 Protobuf，再跑 JSON），避免 CPU 缓存交叉影响

**运行方式**（无需启动服务器）：
```bash
./bin/ser_bench
```

### 2.2 RPC 端到端测试（rpc_bench）

**目的**：测量 Krpc 框架在真实网络环境下的性能表现。

#### 子测试 A：端到端延迟与吞吐

- 在多个并发级别（1, 2, 4, 8, 16, 32 线程）下各发送 N 个请求
- 每个请求经历完整链路：ZK 服务发现 → TCP 连接 → 序列化发送 → 服务端处理 → 接收响应 → 反序列化
- 记录每个请求的延迟，计算 P50/P95/P99/QPS
- 观察随着并发增加，吞吐是否线性增长、延迟是否可控

**运行方式**：
```bash
./bin/rpc_bench -i ./bin/test.conf --concurrency 1,4,8,16,32 --requests 5000
```

#### 子测试 B：连接复用影响

- **模式一（每次新建连接）**：每个请求都执行 socket → connect → send → recv → close，模拟短连接场景
- **模式二（连接复用）**：建立一次连接后连续发送 N 个请求，复用同一 TCP 连接
- 对比两种模式的 QPS 和延迟，量化连接建立/销毁的开销占比

#### 子测试 C：基础设施开销

- **ZK 查询**：仅测量从 Zookeeper 获取服务地址的耗时
- **TCP 连接**：仅测量 socket() + connect() 的耗时
- 这两项帮助定位"不做实际 RPC 调用时，底层基础设施消耗了多少时间"

**运行方式**：
```bash
# 需要先启动服务端: ./bin/server -i ./bin/test.conf
./bin/rpc_bench -i ./bin/test.conf
```

---

## 三、测试结果

### 3.1 序列化对比结果

```
Message: LoginRequest
  +------------------+------------+------------+------------+
  |                  | Size (B)   | Serialize  | Deserialize|
  +------------------+------------+------------+------------+
  | Protobuf         |         18 |     18.4ms |     43.9ms |
  | JSON             |         34 |    369.0ms |   1291.4ms |
  +------------------+------------+------------+------------+
  | Ratio (JSON/PB)  |       1.89x |      20.09x |      29.40x |
  +------------------+------------+------------+------------+

Message: RegisterRequest
  +------------------+------------+------------+------------+
  |                  | Size (B)   | Serialize  | Deserialize|
  +------------------+------------+------------+------------+
  | Protobuf         |         21 |     18.0ms |     44.7ms |
  | JSON             |         44 |    398.4ms |   1473.0ms |
  +------------------+------------+------------+------------+
  | Ratio (JSON/PB)  |       2.10x |      22.10x |      32.92x |
  +------------------+------------+------------+------------+
```

**结论**：
- Protobuf 体积约为 JSON 的 **1/2**，节省约 47%~52% 的网络带宽
- Protobuf 序列化速度是 JSON 的 **20~22 倍**
- Protobuf 反序列化速度是 JSON 的 **29~33 倍**
- 对于 RPC 场景，每次请求都需要序列化+反序列化，50 万次累积差异达秒级

### 3.2 RPC 端到端结果（最终版）

修复内容：客户端 socket 设置 `SO_REUSEADDR`、服务端启用 `kReusePort`、e2e 测试改为线程级连接复用、`std::cout` 替换为 `LOG()` 以通过 glog 过滤。

| 并发数 | 总请求 | 成功 | 成功率 | P50 | P95 | P99 | QPS |
|--------|--------|------|--------|-----|-----|-----|-----|
| 1 | 5000 | 5000 | **100%** | 0.20ms | 0.29ms | 0.37ms | 2612 |
| 8 | 5000 | 5000 | **100%** | 0.40ms | 0.66ms | 0.80ms | 7662 |
| 32 | 4992 | 4992 | **~100%** | 1.38ms | 2.18ms | 2.57ms | ~14000 |

**分析**：
- 所有并发级别下成功率 100%，无 connect/recv/parse/send 失败。
- 单线程 P50=0.20ms，P99=0.37ms，QPS=2612。
- 并发=8 时 QPS 增长到 7662（2.9x），P99 仍在 0.80ms 以内。
- 并发=32 时延迟略有上升（P99=2.57ms），但吞吐量显著提升。

### 3.2.1 修复前的对比（短连接模式）

| 并发数 | 总请求 | 成功 | 成功率 | QPS |
|--------|--------|------|--------|-----|
| 1 | 5000 | 5000 | 100% | 2612 |
| 8 | 5000 | 2771 | 55% | — |
| 32 | 5000 | 1515 | 30% | — |

**失败原因**：所有失败均为 `connect()` 失败（非 recv/parse/send 错误）。每次请求新建连接导致 TIME_WAIT 端口耗尽，服务端 listen backlog 溢出。

**分析**：
- **单线程（并发=1）**: 成功率 100%，P50=0.20ms，P99=0.37ms，QPS=2612。这是最理想的场景，没有并发竞争。
- **低并发（2~4）**: QPS 线性增长到 6430，但成功率开始下降到 80%+。说明服务端出现了并发问题。
- **中等并发（8~16）**: 大量请求失败，服务端可能出现了 socket fd 竞争或缓冲区溢出。
- **高并发（32）**: 成功率又回到 82%，但延迟显著上升（P99=2.57ms）。QPS 计算值高是因为只计算了成功的请求。

### 3.3 连接复用结果

| 模式 | P50 | P95 | QPS |
|------|-----|-----|-----|
| 每次新建连接 | 191μs | 269μs | 2787 |
| 连接复用 | 132μs | 245μs | 6747 |

**结论**：连接复用带来 **2.4x QPS 提升**。每次新建连接需要经历 ZK 查询 + TCP 三次握手，是单次 RPC 调用的主要开销。

### 3.4 基础设施开销

| 操作 | P50 | P95 |
|------|-----|-----|
| ZK 查询 | 207μs | 323μs |
| TCP 连接 | 137μs | 217μs |
| 合计 | ~344μs | ~540μs |

单次完整 RPC 调用耗时约 0.20ms（P50），其中 ~0.34ms 来自 ZK+TCP 连接，说明在短连接模式下**连接建立开销超过了实际 RPC 处理时间**。

---

## 四、修复记录

### 4.1 高并发连接失败（已修复）

**现象**：并发度 >1 时，大量请求在 `connect()` 阶段失败。

**原因**：
1. 每次 RPC 调用都新建连接 → 快速关闭 → TIME_WAIT 端口耗尽
2. 客户端 socket 未设置 `SO_REUSEADDR`
3. 服务端 listen backlog 过小（默认值）

**修复**：
- `Krpcchannel.cc`: `newConnect()` 中设置 `SO_REUSEADDR`（`setsockopt(SO_REUSEADDR)`）
- `Krpcchannel.h/cc`: 新增 `set_reuse_connection(bool)` API，允许调用方选择是否复用连接
- `Krpcchannel.cc`: 析构函数中清理未关闭的 socket
- `Krpcprovider.cc`: TcpServer 启用 `kReusePort` 选项
- `Krpcchannel.cc`: 所有 `std::cout` 错误/调试输出替换为 `LOG(ERROR)`/`LOG(INFO)`，使其可通过 glog `FLAGS_minloglevel` 过滤

### 4.2 已改进

- **基准测试输出清洁化**：`rpc_bench.cc` 中设置 `zoo_set_debug_level(ZOO_LOG_LEVEL_ERROR)` 和 `FLAGS_minloglevel = 1`，屏蔽 Zookeeper 和 glog INFO 日志
- **框架日志统一**：`Krpcchannel.cc` 中 `std::cout` 全部替换为 glog `LOG()` 宏，使用者可通过 `FLAGS_minloglevel` 统一控制日志输出
- **连接复用支持**：`KrpcChannel` 支持 `set_reuse_connection(true)`，生产环境建议启用以避免频繁连接建立的开销

---

## 五、测试环境

| 项目 | 值 |
|------|-----|
| OS | Ubuntu 20.04 LTS (WSL2) |
| 内核 | Linux 5.15.167.4-microsoft-standard-WSL2 |
| C++ 编译器 | GCC 9.4.0 |
| C++ 标准 | C++11 |
| Protobuf 版本 | 3.21.12 |
| 服务端配置 | 4 个 Muduo 线程，IP 127.0.0.1:8000 |
| Zookeeper | 3.4.13, 127.0.0.1:2181 |
| JSON 库 | jsoncpp 1.7.4 |
